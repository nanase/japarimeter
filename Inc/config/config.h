#ifndef __CONFIG_H
#define __CONFIG_H

/* Buttons ------------------------------ */

// Lレベルが維持する最小時間(ms)
#define BUTTON_MIN_LOW_TIME 50

// Hレベルが維持する最小時間(ms)
#define BUTTON_MIN_HIGH_TIME 25

// 長押しと判定される時間(ms)
#define BUTTON_LONG_LOW_TIME 750

#endif
